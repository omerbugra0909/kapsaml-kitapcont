@model List<Ogrenci>

<style>
    body {
        background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
        min-height: 100vh;
        font-family: 'Segoe UI', sans-serif;
    }

    .title {
        color: white;
        font-weight: 700;
        letter-spacing: 1px;
    }

    .add-btn {
        border-radius: 12px;
        font-weight: 600;
        padding: 10px 18px;
    }

    .student-card {
        border: none;
        border-radius: 18px;
        backdrop-filter: blur(10px);
        background: rgba(255,255,255,0.08);
        color: white;
        transition: all .25s ease;
    }

    .student-card:hover {
        transform: translateY(-6px);
        box-shadow: 0 12px 30px rgba(0,0,0,.4);
    }

    .student-name {
        font-weight: 700;
        font-size: 20px;
    }

    .info-text {
        opacity: .85;
        font-size: 14px;
    }

    .btn-custom {
        border-radius: 10px;
        font-size: 13px;
        font-weight: 600;
        padding: 6px 14px;
    }
</style>


<div class="container py-5">

    <div class="d-flex justify-content-between align-items-center mb-5">

        <h2 class="title">🎓 Öğrenci Paneli</h2>

        <a asp-action="Create" class="btn btn-success add-btn">
            + Yeni Öğrenci
        </a>

    </div>


    <div class="row g-4">

        @foreach (var ogrenci in Model)
        {
            <div class="col-md-4">

                <div class="card student-card shadow-lg h-100">

                    <div class="card-body d-flex flex-column justify-content-between">

                        <div>
                            <div class="student-name">
                                @ogrenci.Ad @ogrenci.Soyad
                            </div>

                            <div class="info-text mt-3">
                                ID: @ogrenci.Id <br />
                                Yaş: @ogrenci.Yas
                            </div>
                        </div>


                        <div class="d-flex gap-2 mt-4">

                            <a asp-action="Edit"
                               asp-route-id="@ogrenci.Id"
                               class="btn btn-primary btn-custom w-100">
                                Düzenle
                            </a>

                            <a asp-action="Delete"
                               asp-route-id="@ogrenci.Id"
                               class="btn btn-danger btn-custom w-100">
                                Sil
                            </a>

                        </div>

                    </div>

                </div>

            </div>
        }

    </div>

</div>










    